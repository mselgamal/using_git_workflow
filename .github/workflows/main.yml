name: release-please
on: [push, pull_request]

jobs:
  run-shell-cmd:
      runs-on: ubuntu-latest
      steps:
        - name: echo a string
          run: echo "Hello world"
        - name: multiline
          run: |
            node -v
            npm -v